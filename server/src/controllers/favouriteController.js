import { successResponse } from "../utils/sanitizeResponse.js";
export const createFavouriteController = (service) => ({ list: async (req, res) => successResponse(res, "Saved properties loaded successfully.", await service.list(req.userDocument._id, req.validated.query)), add: async (req, res) => successResponse(res, "Property saved successfully.", { favourite: await service.add(req.userDocument._id, req.validated.params.propertyId) }, 201), remove: async (req, res) => { await service.remove(req.userDocument._id, req.validated.params.propertyId); return successResponse(res, "Saved property removed successfully."); } });
